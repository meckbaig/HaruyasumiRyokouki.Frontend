# API base path. Keep /v1 in development — vite proxies it to BACKEND_ORIGIN.
VITE_API_BASE_URL=/v1

# Proxy target for /v1 in development (never bundled).
BACKEND_ORIGIN=http://localhost:5101

# Map tiles. Defaults to CARTO Voyager (keyless). To get latin/English labels,
# swap in a keyed provider, e.g. MapTiler or Stadia Maps, and set both lines.
# VITE_MAP_TILE_URL=https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png
# VITE_MAP_ATTRIBUTION=&copy; OpenStreetMap &copy; CARTO

# Developer links rendered in the footer.
VITE_AUTHOR_NAME=meckbaig
VITE_AUTHOR_GITHUB=https://github.com/meckbaig
